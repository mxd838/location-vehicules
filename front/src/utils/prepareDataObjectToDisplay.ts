// from data coming from back
// remove values which are not to be displayed in cells
export const prepareDataObjectToDisplay = (
  originalObject: any,
  valuesToRemove: any
) => {
  console.log(originalObject)
  console.log(valuesToRemove)
}
